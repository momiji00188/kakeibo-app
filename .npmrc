# pnpm v11 セキュリティ設定
# npm supply chain attack 対策

# インストールスクリプトを実行しない（明示的に許可したもの以外）
ignore-scripts=false

# lockfileがない場合はインストールを止める（意図しない依存の混入防止）
frozen-lockfile=false

# パッケージのサイドエフェクトキャッシュを有効化
side-effects-cache=true

# node_modules への直接アクセスを制限（ゴーストdeps防止）
hoist=false
shamefully-hoist=false
